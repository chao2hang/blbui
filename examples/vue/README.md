# BLBUI Vue playground

This is a small, real Vite + Vue 3 application covering page layout, tabs,
filter input, table, pagination and dialog state. Install its package manifest
and run `npm run dev` to validate the published Vue adapter independently.
The parity screen also exercises the shared async data contract: error/retry,
permission-denied and a named permission slot.
