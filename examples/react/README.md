# BLBUI React playground

This is a deliberately small, real Vite application covering page layout, tabs,
filter input, table, pagination and dialog state. Install its package manifest
and run `npm run dev` to validate the published React adapter independently.
